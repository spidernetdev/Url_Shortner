Short URL Backend
A Node.js backend service for generating and managing short URLs.

This project allows users to create short links that redirect to original URLs. It is built with Node.js, Express.js, and uses MongoDB as the database for storing URL mappings.

